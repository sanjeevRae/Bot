import Engagement from '../components/Engagement';

/** /engagement — Inbox, Bookings and Leads together under one navbar item, in tabs. */
export default function EngagementPage() {
  return <Engagement />;
}